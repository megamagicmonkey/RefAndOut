using System;

namespace RefandOut
{
    class Program
    {
        //TODO impliment this ref method by returning  a string and mulitplying the parameter.
        //So it will have two outputs. The string will return the multiplication in sentence form. Also set x to equal itself times 5.
        //TODO Call the method in the main and make sure the method changes the number of a initialized variable and returns a string.
        public static string TimesFive(ref int x)
        {

        }

        //TODO create method that uses the out keyword. Call the method in the main.

        static void Main(string[] args)
        {

        }

    }
}
